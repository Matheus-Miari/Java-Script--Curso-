// Criando variavel / VAR ou Let

// var/let IDENTIFCADOR = valor;

// var idade = 25;

let preco = 19.90;
let desconto = 0.4;

let precoComDesconto = preco * (1 - desconto);

console.log(preco * (1 - desconto));
console.log(precoComDesconto)